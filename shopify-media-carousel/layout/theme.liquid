<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title }}</title>
    <link rel="stylesheet" href="{{ 'thumbnail-carousel.css' | asset_url }}">
    <script src="{{ 'thumbnail-carousel.js' | asset_url }}" defer></script>
    {{ content_for_header }}
</head>
<body>
    {{ content }}
</body>
</html>